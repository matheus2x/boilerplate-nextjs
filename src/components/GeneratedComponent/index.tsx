import * as S from './styles';
import * as P from './protocols';

const GeneratedComponent = ({
  title = 'GeneratedComponent',
}: P.GeneratedComponent) => (
  <S.Wrapper>
    <h1>{title}</h1>
  </S.Wrapper>
);

export default GeneratedComponent;
